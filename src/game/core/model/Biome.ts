import { elementTypes, type ElementType } from './Element.ts'

/** Биом пока тематически совпадает с одним из пяти типов элементов. */
export type BiomeType = ElementType

export const biomeTypes = elementTypes
